<UserControl x:Class="NewGame.UI.Controls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Converters="clr-namespace:NewGame.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="140"
             MouseLeftButtonDown="OnCardClick"
             MouseMove="OnCardMouseMove"
             MouseLeftButtonUp="OnCardMouseUp"
             Tag="{Binding}">

    <Border x:Name="CardBorder"
            Background="{Binding Type, Converter={StaticResource TypeToColorConverter}}"
            BorderThickness="3"
            CornerRadius="8"
            Padding="6"
            Cursor="Hand">

        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Name & Mana Cost -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Name}"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="White"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>

                <Border Grid.Column="1"
                        Background="#1E64C8"
                        CornerRadius="10"
                        Padding="6,2"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding ManaCost}"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Card Art Area -->
            <Border Grid.Row="1"
                    Background="{Binding Element, Converter={StaticResource ElementToColorConverter}}"
                    CornerRadius="4"
                    Margin="0,6,0,6"
                    Height="70">
                <TextBlock Text="{Binding Type}"
                           FontSize="12"
                           FontStyle="Italic"
                           Foreground="#80FFFFFF"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- Stats -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            Visibility="{Binding Type, Converter={StaticResource IsCreatureConverter}}">
                    <TextBlock Text="PWR:" Foreground="#FF9632" FontSize="10"/>
                    <TextBlock Text="{Binding Power}" Foreground="#FF9632" FontSize="10" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Visibility="{Binding Type, Converter={StaticResource IsCreatureConverter}}">
                    <TextBlock Text="HP:" Foreground="#64FF64" FontSize="10"/>
                    <TextBlock Text="{Binding Health}" Foreground="#64FF64" FontSize="10" FontWeight="Bold"/>
                </StackPanel>
            </Grid>

            <!-- Element & Rarity -->
            <Grid Grid.Row="3" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Element}"
                           FontSize="9"
                           Foreground="#B0B0B0"/>

                <TextBlock Grid.Column="1"
                           Text="{Binding RarityText}"
                           FontSize="9"
                           Foreground="{Binding Rarity, Converter={StaticResource RarityToColorConverter}}"
                           FontWeight="Bold"/>
            </Grid>
        </Grid>
    </Border>

    <UserControl.Resources>
        <Converters:TypeToColorConverter x:Key="TypeToColorConverter"/>
        <Converters:ElementToColorConverter x:Key="ElementToColorConverter"/>
        <Converters:RarityToColorConverter x:Key="RarityToColorConverter"/>
        <Converters:IsCreatureConverter x:Key="IsCreatureConverter"/>
    </UserControl.Resources>

</UserControl>
